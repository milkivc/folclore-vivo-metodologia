name: Release manual bloqueada para revisão

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  bloqueio-de-publicacao:
    runs-on: ubuntu-latest
    steps:
      - name: Estado
        run: |
          echo "Publicação automática bloqueada."
          echo "Nenhuma release será criada por este workflow."
          echo "Qualquer nova publicação exige validação humana e decisão explícita fora desta automação."
